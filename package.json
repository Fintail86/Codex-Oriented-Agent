{
  "name": "cosia",
  "version": "0.1.0",
  "description": "COSIA: Codex-Oriented Self-Improving Agent Runtime CLI MVP with scoped memory, policy-gated tools, and Codex CLI provider.",
  "type": "module",
  "bin": {
    "cosia": "./dist/src/cli.js",
    "agent-runtime": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
